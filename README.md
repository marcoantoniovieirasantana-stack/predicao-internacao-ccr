# Aplicação Streamlit — CCR

Arquivos gerados automaticamente pelo notebook final.

## Execução local

```bash
pip install -r requirements.txt
streamlit run app.py
```

## Arquivos obrigatórios

- `app.py`
- `27_deployment_bundle.joblib`
- `requirements.txt`

O arquivo `27_deployment_bundle.joblib` contém o pipeline final, preditores, threshold operacional e metadados da interface.
